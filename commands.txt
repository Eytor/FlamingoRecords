docker network create flamingo-network

# api-gateway
docker build --tag eythoratli/api-gateway --file api-gateway/Dockerfile .

docker run -d -p 7000:7000 --network=flamingo-network --name api-gateway -t eythoratli/api-gateway

# customer-service
docker build --tag eythoratli/customer-service --file customer-service/Dockerfile .

docker run -d --network=flamingo-network --name customer-service -t eythoratli/customer-service

docker run --network=flamingo-network --name customer-db -e POSTGRES_PASSWORD=Abc.12345 -e POSTGRES_DB=customer_db -d postgres

# order service
docker build --tag eythoratli/order-service --file order-service/Dockerfile .
docker run -d --network=flamingo-network --name order-service eythoratli/order-service

# order database
???

# log-service
docker build -t eythoratli/log-service --file log-service/Dockerfile .
docker run -dit --rm --network=flamingo-network --name log-service eythoratli/log-service

# email service
docker build -t eythoratli/email-service --file email-service/Dockerfile .
docker run -dit --rm --network=flamingo-network --name email-service eythoratli/email-service
